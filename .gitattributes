# 行尾统一为 LF（仓库内与检出后都是 LF），避免跨平台协作时的行尾噪音。
# 本文件显式覆盖 core.autocrlf=true 的默认行为（Windows PortableGit 默认开启该选项）。
* text=auto eol=lf

# 二进制资源：不做行尾转换，也不生成行内 diff。
*.png   binary
*.jpg   binary
*.jpeg  binary
*.webp  binary
*.gif   binary
*.ico   binary
*.woff  binary
*.woff2 binary
