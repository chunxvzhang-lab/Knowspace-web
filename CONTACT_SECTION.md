# 页脚联系区块 · 交付说明

交付日期：2026-09-11

---

## 一、需求

> 在页脚下方增加联系方式（邮箱 `chunxvzhang@gmail.com`），并把 Telegram 图片一并放进联系区块。

---

## 二、交付内容

页脚在「4 列链接栏」与「版权栏」之间新增一个独立联系区块，桌面为**两栏布局**：

| 位置 | 内容 |
|---|---|
| 左 | Telegram 二维码卡片（240px，悬停微浮起；点击直接打开 `t.me` 对话） |
| 右上 | Mail 图标 + 「联系我们」标题 + 说明文案 |
| 右下 | 两行渠道：`chunxvzhang@gmail.com`（`mailto:`）· `Telegram @MOYUING_404`，各带一键复制 |

移动端（≤768px）自动堆叠为纵向，二维码居中。

### 资源处理

`Downloads/Telegram.jpg`（667×850，357.4 KB）→ `public/telegram-card.webp`

- **480×612，43.3 KB，体积 −88%**
- 原图备份：`.workbuddy/assets/telegram-card-original.jpg`

---

## 三、关键决策

### 1. 二维码内容先解码确认，再谈美化

用 `zxing-cpp` 解出图中二维码实际编码 `https://t.me/moyuing_404`，**确认可用后才集成**。该 URL 在 `Footer.tsx` 中固化为 `TELEGRAM_URL` 常量，并在注释中标明「必须与本图编码内容保持一致」。

### 2. 压缩方案以「可扫描」为准，而非只看体积

对多个尺寸/质量组合逐一做 ZXing 实测，选定 **480px / q92（43.3 KB）**；并验证**缩到实际展示尺寸仍能解码**（480→160px 全部通过，余量充足）。

> 二维码在源图中仅占卡片宽度的 **47.5%**——卡片尺寸直接决定可扫描性，这是选 240px 而非更小值的依据。

### 3. 桌面尺寸给足，移动端适度

真正的扫码场景是**桌面访客用手机扫屏幕**；手机访客直接点链接即可。因此：

| 断点 | 卡片 | 二维码实渲染 |
|---|---|---|
| 桌面 | 240px | ~114px |
| ≤768px | `min(220px, 100%)` | ~104px |

`min()` 用于保证 320px 窄屏不溢出。

---

## 四、顺带修复的缺陷（英文模式整页横向滚动）

验证英文文案时发现 **1440px 视口下整页横向溢出 132px**（中文为 0）。**此为预存在问题，与本次改动无关。**

### 根因（数据定位）

`.sticky-nav-inner` 的 `max-width: 1360px` 使导航内容区**恒为 1312px，与屏幕宽度无关**。而三块 flex 子项均为 `flex-shrink: 0`：

| 语言 | 品牌块 | 导航链接 | 控件块 | 合计（含 gap） | 所需视口 |
|---|---|---|---|---|---|
| 中文 | 265 | 548 | 481 | 1326px | 1374px |
| 英文 | 265 | 639 | 571 | 1507px | 1555px |

缺口：中文 14px、英文 195px —— 与实测的 14px / 196px 完全吻合。

### 修复

1. `.sticky-nav-inner` 上限 `1360px → 1600px`
2. 区块锚点折叠断点 `1220px → 1560px`：**锚点是便捷捷径（页脚有同样目标），主导航「产品特性 / 文档」必须永远可见**
3. `.nav-links-container` 增加 `flex-shrink: 1` + 隐藏滚动条作为兜底

---

## 五、验证结果

| 项 | 结果 |
|---|---|
| `tsc && vite build` | ✅ 通过（1575 模块） |
| 纯静态托管 `dist/` 全部引用 | ✅ 200，字节数与磁盘一致 |
| 浏览器实际渲染像素 → ZXing 解码 | ✅ desktop / mobile / narrow320 **全部 PASS** |
| 图片渲染比例 vs 原图 | ✅ `0.7843` vs `0.7843`（无畸形拉伸） |
| **15 个视口宽度 × 中英双语** | ✅ 页面横向溢出**全部为 0** |
| 导航完整性 | ✅ ≥1561px 显示全部 6 项；1560–1100px 折起锚点但主导航与控件完整 |
| 控制台错误 / 4xx 响应 | ✅ 均为空 |
| 中英文案（各 1 行，不破版） | ✅ |

---

## 六、遗留项（未在本次处理）

- **小屏导航仍会横向滚动**：≤1100px(en) / ≤900px(zh)。属预存在缺口——移动端没有汉堡菜单，超出本次改动范围。
- **无 URL 路由**：`activeView` 为内存 state，刷新或分享链接会回到 landing。修复需引入路由或 hash 同步，待确认。

---

## 七、相关文件

| 文件 | 说明 |
|---|---|
| `src/components/Footer.tsx` | 联系区块主体；顶部三个常量 `CONTACT_EMAIL` / `TELEGRAM_HANDLE` / `TELEGRAM_URL` |
| `public/telegram-card.webp` | 二维码卡片（480×612，43.3 KB） |
| `src/index.css` | `.footer-contact*` 段 + 导航宽度修复 |
| `src/i18n/types.ts` · `translations.ts` | 新增 6 个文案字段（zh/en 成对） |
| `docs/preview/preview-footer-contact*.png` | 桌面 / 移动端预览 |
